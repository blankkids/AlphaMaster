# 通达信数据源修复 — 市场判定 + 分页拉取

## 日期
2026-08-03

## 目标
修复 `D:\cl\AlphaMaster\web\data_sources\tongdaxin_source.py` 的两个核心缺陷，使通达信数据源能正确识别 A 股市场并拉取足够历史数据。

## 问题分析

### 问题 1：`_parse_market` 盲目信任 sh/sz 前缀
- **现象**：`sh002080`（中材科技，深市中小板）被当成沪市去查 → 返回"通达信无数据"
- **根因**：原逻辑先检查 `SH`/`SZ` 前缀并直接返回，不校验代码本身是否匹配该市场
- **影响**：所有 002/300/688 等代码若前缀写反，都会查不到数据

### 问题 2：`fetch_bars` 截断到 800 条
- **现象**：信号引擎需要 ≥3000 条历史数据，但 pytdx 单次上限 800 条
- **根因**：`want = min(max(n + 2, 20), 800)` 硬截断，未实现分页
- **影响**：所有通达信监控一直 `insufficient`，算不出信号

## 修改内容

### 改动 1：重写 `_parse_market` — 市场自动纠正
- 优先按代码本身判定市场：
  - `002/003/300/301` → 深市中小板/创业板
  - `60/688/689` → 沪市主板/科创板
  - `5/9/11/13` → 沪市基金/B股/转债
  - `399` → 深证指数
- 仅 `000xxx`（上证指数 vs 深市股票）歧义时才以 sh/sz 前缀为准
- 用户写 `sh002080` 会自动纠正为深市（market=0）

### 改动 2：新增 `_fetch_paginated` — 分页拉取
- 每页 800 条，通过递增 `start` 偏移量逐页索取
- 按 `datetime` 去重（不同页可能重叠）
- 本页返回不足 page_size 或全部重复时停止
- `fetch_bars` 的 `want` 不再截断到 800

### 改动 3：`_fetch_raw` 增加 `start` 参数
- 原来硬编码 `start=0`，现在支持分页偏移

## web_settings.json
当前文件只有 2 条 MT5/XAUUSD 监控，无通达信监控项，无需清理重复/错误项。

## 验证结果
- 语法检查通过
- 12 个 `_parse_market` + `_is_index` 测试用例全部 PASS，覆盖：
  - `sh002080` → 深市（自动纠正）
  - `sh000001` → 沪市指数
  - `sz000001` → 深市股票
  - 各类无歧义代码（002/300/60/688/399）

## 未修改
- `web_settings.json` — 无通达信监控项，无需清理
